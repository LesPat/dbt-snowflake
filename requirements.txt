# Reference dependencies for local development (non-Docker).
# The Airflow Docker image uses data_extraction_dag/requirements.txt.

apache-airflow==2.9.3
snowflake-connector-python==3.18.0
dbt-snowflake>=1.8.0
requests>=2.31.0
